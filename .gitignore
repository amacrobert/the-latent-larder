_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/
.idea/
.DS_Store

# Full-size source images. The published site uses the WebP derivatives
# alongside them; these stay local so git history isn't carrying megabytes.
assets/images/originals/
